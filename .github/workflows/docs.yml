name: Docs

on:
  push:
    branches: [main]
    paths:
      - docs/**
      - crates/rf24-rs/**
      - '**/*.md'
      - .github/workflows/docs.yml
  pull_request:
    branches: [main]
    paths:
      - docs/**
      - crates/rf24-rs/**
      - '**/*.md'
      - .github/workflows/docs.yml

jobs:
  supplemental:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - uses: actions/setup-node@v4
        with:
          node-version: 20.x
      - run: yarn install
      - name: Build Node.js type definitions
        run: yarn build:debug
      - name: Generate Node.js API docs
        run: yarn docs
      - run: uv sync --group docs
      - run: uv run mkdocs build --config-file docs/mkdocs.yml --strict
      - name: Save docs build as artifact
        uses: actions/upload-artifact@v4
        with:
          path: docs/site
          name: supplemental-docs

  api:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Rust
        run: rustup update --no-self-update
      - run: cargo doc --no-deps --lib -p rf24-rs -p rf24ble-rs
      - name: save docs as artifact
        uses: actions/upload-artifact@v4
        with:
          name: api-docs
          path: target/doc
